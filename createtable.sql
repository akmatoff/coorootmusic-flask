CREATE TABLE releases (
    id integer primary key AUTOINCREMENT,
    artist varchar(50),
    title varchar(50),
    genre varchar(50),
    album varchar(50),
    cover varchar(50),
    pub_date varchar(50)
);
